class CloudinaryConfig {
  CloudinaryConfig._();

  // ── Fill these in ──────────────────────────────────────────────────────
  // 1. Go to cloudinary.com → Settings → Upload → Upload presets
  // 2. Create an UNSIGNED preset, copy its name below.
  // 3. Copy your Cloud Name from the dashboard top-left.
  static const String cloudName    = 'dkrcpn0sp';
  static const String uploadPreset = 'mysavefood_preset';
  // ──────────────────────────────────────────────────────────────────────
}
